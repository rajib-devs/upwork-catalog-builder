# Proof bank and context (Rajib, WordPress support and server admin)

Use only these verified facts, or facts the user adds in the prompt. Keep numbers exactly as written.

## Positioning
- Upwork title: "WordPress Support, Migration & Malware Removal | Server Admin".
- Support-first positioning: ongoing WordPress support and server administration, with migration, malware removal, speed, and WooCommerce support as core services.
- Hourly rate context: $25/hr. Catalog tier prices should make sense against that rate and the real hours involved.

## Verified proof points
- Malware: cleaned a multi-site WordPress reinfection that survived earlier cleanups. Malware lived in the database and rebuilt deleted files through a scheduled task every 10 minutes. Five separate footholds on the worst site (auto-loaded drop-in files, a must-use plugin, a duplicate plugin copy, theme code injection, a hidden staging folder). 31 malware entries on one site and more than 25 on the other. One hidden admin account per site, named to look like a backup tool. Recovered the malware's own file list to guide cleanup. Both sites scanned clean at file and database level after cleanup. Original entry point not conclusively identified. Do not claim "stayed clean since" unless the user confirms follow-up checks.
- Inode cleanup: reduced a cPanel account from 89% inode usage to 53%.
- Memory diagnosis: found a hidden shared-hosting memory ceiling, 1024M configured but about 640MB real limit.
- PHP 8 migration: fixed 7 fatal errors across 5 widgets.
- AWS: migrated a SugarCRM application from IONOS to AWS (EC2, S3, IAM, EBS, CloudFront, Redis, backups).
- Automation: runs a self-hosted n8n server on AWS (Docker, PostgreSQL, Caddy) that publishes WordPress drafts through the REST API.
- Email: built a 55-mailbox sending setup across 11 domains with full SPF, DKIM, DMARC; traced a case of DKIM being stripped by a proxy; one client rehired him five times for email and deliverability work.
- Experience: WordPress work since 2016, about 10 years in IT and web.

## Catalog status
- Live or submitted: WordPress malware removal and hacked site recovery (see example-malware-offer.md).
- Planned next, in order: WordPress migration to new hosting, monthly WordPress maintenance plan, email deliverability setup (SPF, DKIM, DMARC).

## Writing rules
- Never use em dashes.
- Plain English a stressed business owner understands, with one or two technical details that build trust.
- Calm, competent tone. No hype words like "best", "top", "No.1", "guaranteed".
- No guarantees about third-party outcomes; state what is controlled instead.
